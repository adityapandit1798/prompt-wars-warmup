Generate a COMPLETE, copy-paste runnable index.html file for: {{FEATURE DESCRIPTION}}

FRAMEWORK: Vanilla HTML + CSS + JavaScript (all inline)
USER STORY: "As a {{user}}, I want to {{action}} so that {{benefit}}"

REQUIREMENTS:
- Return ONLY the code inside ```html ... ```
- Include mock data (no external APIs)
- Add error handling for: {{edge case}}
- Max 120 lines total
- Clean, modern UI (flexbox/grid, readable fonts, subtle colors)
- Must work offline in any browser

SELF-CHECK before outputting:
☐ No placeholder comments like "// add logic here"
☐ All tags closed, valid HTML5
☐ JS runs without console errors
☐ Demo flow is obvious to a non-technical judge